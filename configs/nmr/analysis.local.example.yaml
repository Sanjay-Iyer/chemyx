# Copy this file to configs/nmr/analysis.local.yaml and edit it once per laptop.
# The local file is ignored by Git and is merged over the shared analysis.yaml.
# Raw and processed trees below are ignored by Git and can differ by laptop.

input:
  paths:
    - results/raw/nmr/06-09-26

output:
  directory: results/processed/nmr
  run_name: null
