# =====================================================================
# 02_set_diameter.txt  —  SET SYRINGE DIAMETER
# What it does : sets the inner diameter (mm) of the loaded syringe.
# Valid range  : 0.103 to 40.000 mm.
# Expected echo: the pump replies   diameter = 4.5
#   If the echoed number differs from what you typed, the value was out
#   of range and did NOT take — pick a value inside the range.
# Channel (4000X): prefix the channel number, e.g.  "2 set diameter 4.5".
# REMINDER: press Enter (carriage return, \r) after the command line.
# NOTE: lines starting with # are notes for YOU — do NOT type them.
# ------------------- SEND THE LINE BELOW -----------------------------
set diameter 4.5
