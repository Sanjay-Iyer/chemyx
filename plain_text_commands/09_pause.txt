# =====================================================================
# 09_pause.txt  —  PAUSE
# What it does : pauses the current move. Resume later by sending "start"
#   (see 07_start.txt).
# Expected echo: the pump replies   pump pause
# Channel (4000X): prefix the channel number, e.g.  "2 pause".
# REMINDER: press Enter (carriage return, \r) after the command line.
# NOTE: lines starting with # are notes for YOU — do NOT type them.
# ------------------- SEND THE LINE BELOW -----------------------------
pause
