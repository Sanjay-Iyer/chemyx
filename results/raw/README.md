# Local raw data

Place machine-local NMR datasets under `results/raw/nmr/<run>/`.

Git tracks this directory skeleton only. Experimental data files and run
folders inside it are ignored, so laptops may contain different raw datasets
without affecting `git pull` or `git push`.
